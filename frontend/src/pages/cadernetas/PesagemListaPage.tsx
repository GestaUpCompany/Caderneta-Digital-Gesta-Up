import ListaRegistros from '../../components/cadernetas/ListaRegistros'

export default function PesagemListaPage() {
  return (
    <ListaRegistros
      caderneta="pesagem"
      titulo="PESAGEM"
      rotaForm="/caderneta/pesagem"
    />
  )
}
